import std.regex.{ Regex }

fun title(html) -> string! {
    const re = Regex.new("<title>(.+)</title>")!
    return re.find(html).context("no <title> tag")!.group(1).context("no capture group")!.text
}

fun main() {
    const text = "<html><body>No title</body></html>"
    println(title(text)!)
}
