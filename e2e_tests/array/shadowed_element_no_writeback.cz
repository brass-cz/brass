// Assigning a shadowing local must not write its value back into the outer
// for-loop element's array slot.
fun main() {
    let values = [1]
    for value in values {
        let value = 10
        value = 11
    }
    println(values)
}
