---
// The full-screen playground: editor on the left, program output on the
// right. A standalone page outside the Starlight docs chrome, mirroring the
// original mdbook playground layout.
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <title>prepoly Playground</title>
  </head>
  <body>
    <div id="playground" data-prepoly-playground>
      <section>
        <div class="pp-editor" data-editor></div>
      </section>
      <section>
        <button class="pp-exec" data-exec>Execute</button>
        <h3>stdout</h3>
        <div class="pp-out" data-stdout></div>
        <h3>stderr</h3>
        <div class="pp-out pp-err" data-stderr></div>
      </section>
    </div>
  </body>
</html>

<style>
  :root {
    --bg-color: #222224;
    --color: #efefef;
  }
  body,
  html {
    width: 100%;
    height: 100%;
    margin: 0;
    padding: 0;
    color: var(--color);
    background: var(--bg-color);
    font-family: sans-serif;
  }
  * {
    box-sizing: border-box;
  }
  #playground {
    display: flex;
    height: 100%;
    padding: 8px;
  }
  #playground > section {
    width: 50%;
    height: 100%;
    padding: 12px;
  }
  .pp-editor {
    width: 100%;
    height: 100%;
    border: 1px solid #ccc;
  }
  .pp-exec {
    width: 100%;
    margin: 8px 0;
    padding: 8px;
    font-size: 1.2em;
    color: var(--color);
    background: var(--bg-color);
    border: 1px solid #ccc;
    cursor: pointer;
  }
  .pp-out {
    margin: 8px 0;
    padding: 12px;
    border: 1px solid #ccc;
    font-size: 1.2em;
    overflow-x: auto;
  }
  .pp-err {
    color: #c88;
  }
  .pp-out :global(pre),
  .pp-out :global(code) {
    margin: 0;
    padding: 0;
  }
</style>

<script>
  import { mountPlayground } from "../playground/playground";

  for (const el of document.querySelectorAll<HTMLElement>("[data-prepoly-playground]")) {
    mountPlayground(el);
  }
</script>
